import { UserType } from "@shared/types";

export const loginGoogleUser = async (
  refreshToken: string,
): Promise<UserType> => {};

export const registerGoogleUser = (
  refreshToken: string,
): Promise<UserType> => {};
